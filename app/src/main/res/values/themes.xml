<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/brand</item>
        <item name="colorPrimaryVariant">@color/brand</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="calculatorButton">
        <item name="android:textSize">@dimen/taxtbutton</item>
    </style>

    <style name="calculatorButton.number">
        <item name="background">@drawable/circle_button_white</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="calculatorButton.Operatoin">
        <item name="background">@drawable/circle_button</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <color name="black">#000</color>
    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
</resources>